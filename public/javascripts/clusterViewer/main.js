define(["jquery", "jquery-ui", "jquery.unige.clusterViewer"], function($, undefined, undefined) {
//define(["jquery-ui", "jquery.unige.clusterViewer"], function($, cluster) {
    //the jquery.alpha.js and jquery.beta.js plugins have been loaded.
    $(function() {
          $('#clusterViewer').clusterViewer();
    });
});
